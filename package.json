{
  "name": "awcp-agent-console",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "AWCP Agent Console — React dashboard for the AWCP gateway (select an agent, run a prompt, watch the live step timeline). Self-contained: delete this folder and the backend is unaffected.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 5173"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^5.4.11"
  }
}
